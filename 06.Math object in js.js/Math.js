// Math = built in js object that provide a collection
        //    of properties and methods

console.log(Math.PI);

let x = 3.56;
let a = 3.99;
let b = 3.1;
let c = 3.21;
let d = 3;
let e = 81;
let f = 45;
let g = -4;
let y = 2;
let z;

// z = Math.round(x);
// z = Math.floor(a); // will always round down
// z = Math.ceil(b);  // opposite of floor will always round up
// z = Math.trunc(c); // eliminate any decimal portion
// z = Math.pow(y,d); // raise a base to a given power
// z = Math.sqrt(e); // square root of
// z = Math.log(e); // natural logarithm
// z = Math.sin(f);
// z = Math.cos(f);   // trignaometry with in the function you will place radians
// z = Math.tan(f);
// z = Math.abs(g);  //// absolute value of a number
// z = Math.sign(g);  // find sign of // -1

let max = Math.max(a,b,c,d,e,f,g,x,y); //didn't put z since it is not assigned
let min = Math.min(a,b,c,d,e,f,g,x,y); 


console.log(z);
console.log(max);
console.log(min);
