// logical operators = used to combine or manupulate boolean values 
                        // (true or false)


                        // AND = && // both condition is to be true
                        // OR = ||  // at least one of the condition should be true
                        // NOT = ! //

// const temp = 20;

// if (temp>0 && temp<= 30) {
//     console.log ("The weather is Good");
// }
// else{
//     console.log("The weather is Bad");
// }
const isSunny = true;
if (!isSunny){
    console.log('IT is sunny');
}
else {
    console.log("It is not SUNNY");
}